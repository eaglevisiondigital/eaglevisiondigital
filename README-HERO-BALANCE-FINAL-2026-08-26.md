# Eagle Vision Digital — FreeWebsite Hero Balance Final

This package applies the latest requested hero/layout fixes:

- Enlarged and better-contained hero device showcase so it fills more of the hero area without running off the right edge.
- Wider paragraph measure under the main headline so the copy wraps into fewer lines and balances better with the heading.
- Rebalanced conversion row so the two primary CTA buttons sit higher, the micro-note sits directly below them, and the FAQ button is larger and centered below.
- Further left/right justification of the freewebsite header so the logo sits farther left and the Main Website link sits farther right.
- Responsive guardrails preserved so the hero image stays fully visible across breakpoints.

Primary changed file:
- `css/freewebsite.css`
